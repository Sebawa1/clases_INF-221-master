/**
 * @file max-sum.h
 * @brief Definitions for max-sum
 */

#ifndef MAX_SUM_H
#define MAX_SUM_H

double MaxSum(double[], int);

static inline double max(double a, double b) { return (a > b) ? a : b; }

#endif
